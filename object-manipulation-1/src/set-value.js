/* exported setValue */

// define a function named setValue with three parameters: object, key, value - complete
// access object at key
// assign the value to the object at key

function setValue(object, key, value) {
  object[key] = value;
}
