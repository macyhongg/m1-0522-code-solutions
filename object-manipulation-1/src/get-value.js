/* exported getValue */

// define a function named getValue with two parameters, object and key - completed
// access the value in object found at the key property - completed
// return the value in object found at the key property - completed


function getValue(object, key){
  return object[key];
}
